// 클래스는 객체를 생성하기 위한 템플릿이다.
class Person {
    // 클래스 필드(변수)
    String name; // 인스턴스 변수

    void introduce(String greeting) { // 지역 변수
        System.out.println(greeting + ", my name is " + name);
    }
}

